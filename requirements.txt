# Core
PyYAML>=6.0

# Scraper — Playwright renders JS so the authenticated LinkedIn session works.
# After installing, run once:  playwright install chromium
Scrapy>=2.11
playwright>=1.40
scrapy-playwright>=0.0.34

# Dashboard
Flask>=3.0
