# Core
PyYAML>=6.0

# Scraper
Scrapy>=2.11

# Dashboard
Flask>=3.0

# Nice-to-have for LinkedIn (JS-rendered pages need a real browser session).
# Optional — only needed if you actually attempt LinkedIn scraping.
# playwright>=1.40
# scrapy-playwright>=0.0.34
